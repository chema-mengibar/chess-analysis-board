
```
        // cy.get(buttonSelector)
        //     .trigger('mousedown', { eventConstructor: 'MouseEvent' })
        //     .trigger('mouseup', { eventConstructor: 'MouseEvent' })

        // .trigger('mousedown', { which: 1, button: 0 })
        // .trigger('mouseup', { button: 0 })





        // const $button = Cypress.$(buttonSelector)[0];
        // $button.dispatchEvent(new MouseEvent('mousedown'));
        // cy.wait(1000)
        // $button.dispatchEvent(new MouseEvent('mouseup'));

        // cy.get(buttonSelector).dispatchEvent(new MouseEvent('mousedown'));
        // cy.wait(1000)
        // cy.get(buttonSelector).dispatchEvent(new MouseEvent('mouseup'));



        // , #base-a8
```